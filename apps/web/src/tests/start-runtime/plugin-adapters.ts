/**
 * `#tanstack-start-plugin-adapters` for the test run. No Start plugin in this
 * app contributes serialization adapters, so there is nothing to register.
 */
export const hasPluginAdapters = false

export const pluginSerializationAdapters = []
