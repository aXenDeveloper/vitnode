---
title: Introduction
description: Welcome to the VitNode documentation!
icon: Power
---

<Callout type="warn" title="This documentation is under construction! 🚧">
  We're working hard to bring you the best documentation experience.
</Callout>

## The stack

A VitNode app is one server with two halves, and the line between them is the one
worth memorising:

```txt
TanStack Start  ->  pages, routing, rendering
Hono            ->  the API, and every security decision
```

- **[TanStack Start](https://tanstack.com/start)** on Vite serves the site, the
  AdminCP and the documentation. Pages are **TanStack Router** routes; cached
  data is **TanStack Query**.
- **[Hono](https://hono.dev)** is the API at `/api/*`, with OpenAPI schemas and
  **Drizzle ORM** over PostgreSQL. It authorizes every read and write from the
  session cookie, on every request - a route guard is navigation, never the
  boundary.
- **Plugins** are packages. A plugin declares its URLs in a **route manifest**
  and exports the module that renders each one; the app compiles that list into
  lazy imports at build time. Nothing is copied into your app.
- **[Fumadocs](https://fumadocs.dev)** renders these pages from MDX, served by
  the same app at `/docs/*`.

## Support

- [Postgres 18-19](https://www.postgresql.org/) (min: v18, recommended: v19) - database support.

### Supported Package Managers

- [bun](https://bun.com/) (min: v1.1, recommended: v1.3)
- [pnpm](https://pnpm.io/) (min: v10, recommended: v11)
- [node.js](https://nodejs.org/) (min: v22, recommended: v24)

### Optional Support

- [Redis](https://redis.io/) (min: v7, recommended: v8) - caching and session management.
- [Docker](https://www.docker.com/) (min: v24, recommended: v25) - containerization and deployment.
- [ElasticSearch](https://www.elastic.co/elasticsearch/) (min: v8, recommended: v9) - advanced search capabilities.
- [NodeMailer](https://nodemailer.com/about/) - email sending capabilities.
- [Resend](https://resend.com/) - email sending capabilities.
- [S3](https://aws.amazon.com/s3/) - file storage.
- [Supabase](https://supabase.com/) - database management and file storage.

## Get started

import { Tab, Tabs } from "fumadocs-ui/components/tabs";

<Tabs groupId='package-manager' persist items={['bun', 'pnpm', 'npm']} label='Create a Plugin'>

```bash tab="bun"
bun create vitnode-app@canary
```

```bash tab="pnpm"
pnpm create vitnode-app@canary
```

```bash tab="npm"
npx create-vitnode-app@canary
```

</Tabs>

Then start it:

```bash
pnpm dev
```

Every app that owns a schema prepares the database - generate, apply, seed -
before its dev server accepts a request, so a fresh clone needs no setup command
of its own. See [Database](/docs/dev/database#your-first-start).

## Where to go next

<Cards>
  <Card
    title="TanStack Start"
    description="The web runtime: routing, data, metadata, i18n"
    href="/docs/dev/tanstack"
  />
  <Card
    title="Create a plugin"
    description="From zero to a live page"
    href="/docs/dev/tanstack/plugin"
  />
  <Card
    title="Plugins"
    description="APIs, database tables, content types, AdminCP screens"
    href="/docs/dev/plugins/create"
  />
  <Card
    title="Contribution"
    description="Run VitNode itself, and change these docs"
    href="/docs/dev/contribution"
  />
</Cards>
